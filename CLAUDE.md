@AGENTS.md

<!--
The full working guide lives in AGENTS.md so every tool (Claude Code, Codex,
Cursor, …) reads the same contract. Claude Code picks it up via the @import above.
Project-specific guardrails — the no-RN-in-core rule, the two-surface rule, the
tvOS `Platform.OS === 'ios'` trap, and the done-gate — are all there.
-->
